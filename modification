if (prepared.Count >= batchSize)
{
    FlushImportBatch(
        tableName,
        prepared,
        oldIdPerRow,
        insertCols,
        columnTypes,
        hasIdentity,
        identityColumn,
        ref inserted,
        ref duplicates,
        ref errors);
}

FlushImportBatch(
    tableName,
    prepared,
    oldIdPerRow,
    insertCols,
    columnTypes,
    hasIdentity,
    identityColumn,
    ref inserted,
    ref duplicates,
    ref errors);