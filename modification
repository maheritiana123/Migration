CREATE PROCEDURE [dbo].[batch_select_export]
(
    @TableName NVARCHAR(128),
    @Offset INT,
    @BatchSize INT
)
AS
BEGIN
    SET NOCOUNT ON;

    DECLARE @SQL NVARCHAR(MAX);

    SET @SQL = '
        SELECT *
        FROM ' + QUOTENAME(@TableName) + '
        ORDER BY 1
        OFFSET ' + CAST(@Offset AS NVARCHAR) + ' ROWS
        FETCH NEXT ' + CAST(@BatchSize AS NVARCHAR) + ' ROWS ONLY';

    EXEC sp_executesql @SQL;
END


int offset = 0;
int batchSize = 5000;

while (true)
{
    // EXEC export batch

    int rows = sql.ReturnedRows(1,1);

    if (rows == 0)
        break;

    // écrire CSV

    offset += rows;
}