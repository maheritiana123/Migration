private Dictionary<string, List<ForeignKeyInfo>> BuildFkCache(IEnumerable<string> tables)
{
    var cache = new Dictionary<string, List<ForeignKeyInfo>>(StringComparer.OrdinalIgnoreCase);
    foreach (var t in tables)
        cache[t] = GetForeignKeyInfo(t) ?? new List<ForeignKeyInfo>();
    return cache;
}