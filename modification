using var reader = new StreamReader(csvPath, Encoding.UTF8);

// lecture header
string headerLine = reader.ReadLine();

if (string.IsNullOrWhiteSpace(headerLine))
{
    Console.WriteLine($"CSV vide : {tableName}");
    return;
}

var columnNames = headerLine
    .Split(';')
    .Select(c => c.Trim())
    .ToList();

var fkInfos = GetForeignKeyInfo(tableName);
var identityColumn = GetIdentityColumn(tableName);
bool hasIdentity = !string.IsNullOrEmpty(identityColumn);

var columnTypes = columnNames.ToDictionary(
    c => c,
    c => GetColumnType(tableName, c),
    StringComparer.OrdinalIgnoreCase);

// IMPORTANT : plus de préallocation géante
var prepared = new List<Dictionary<string, object>>();

var oldIdPerRow = new List<string>();

int structureSkipped = 0;

string currentLine;

while ((currentLine = reader.ReadLine()) != null)
{
    var vals = ParseCsvSemicolon(currentLine);

    if (vals.Count != columnNames.Count)
    {
        structureSkipped++;
        continue;
    }

    var row = new Dictionary<string, object>(
        StringComparer.OrdinalIgnoreCase);

    for (int c = 0; c < columnNames.Count; c++)
        row[columnNames[c]] = vals[c];

    string oldId = null;

    if (hasIdentity && row.ContainsKey(identityColumn))
    {
        oldId = (row[identityColumn] ?? "").ToString();

        // ne jamais insérer l'IDENTITY
        row.Remove(identityColumn);
    }

    oldIdPerRow.Add(oldId);

    // ===========================
    // TOUT TON CODE MÉTIER EXISTANT
    // FK / NULL / DEFAULT / FALLBACK
    // etc...
    // ===========================

    prepared.Add(row);
}