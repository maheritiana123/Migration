BEGIN CATCH
    DECLARE @ErrMsg nvarchar(max) = ERROR_MESSAGE();
    DECLARE @ErrNum int = ERROR_NUMBER();
    DECLARE @ErrState int = ERROR_STATE();
    DECLARE @XactState int = XACT_STATE();

    RAISERROR(
        'INSERT batch erreur %d | State=%d | XACT_STATE=%d | %s',
        16,
        1,
        @ErrNum,
        @ErrState,
        @XactState,
        @ErrMsg
    );
END CATCH